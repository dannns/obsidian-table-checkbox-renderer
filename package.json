{
  "name": "table-checkbox-renderer",
  "version": "0.9.1",
  "description": "Obsidian plugin for interactive checkboxes in Markdown tables. Supports multiple checkboxes per cell and robust mapping between rendered checkboxes and source Markdown.",
  "main": "main.js",
  "scripts": {
    "build": "tsc && cp manifest.json build/"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.8.3"
  },
  "dependencies": {
    "obsidian": "*"
  }
}
